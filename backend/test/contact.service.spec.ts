import {ContactService} from "../src/contact/contact.service";describe("ContactService",()=>{it("normalises and persists",async()=>{const create=jest.fn().mockResolvedValue({id:"id",status:"NEW"});const service=new ContactService({contactMessage:{create}} as never);await service.create({fullName:" Jane Doe ",email:"JANE@EXAMPLE.COM",phone:" +94770000000 ",subject:" Hello ",message:" A sufficiently detailed message. "});expect(create).toHaveBeenCalledWith(expect.objectContaining({data:expect.objectContaining({fullName:"Jane Doe",email:"jane@example.com",subject:"Hello"})}))})});
