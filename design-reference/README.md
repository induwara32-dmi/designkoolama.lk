# Design Reference Inventory

The conversation-provided overview screenshot is the current visual source. It shows desktop compositions for fourteen public pages but does not expose legible asset details at full resolution.

Add original Figma exports here using route-oriented names such as `home-desktop.png`, `home-mobile.png`, and `service-brand-identity-desktop.png`. Source exports override any temporary CMS seed content.
