import {PUBLIC_API_URL} from "@/lib/api-config";
export class ApiError extends Error{constructor(message:string,readonly status?:number){super(message)}}
const baseUrl=PUBLIC_API_URL;
export async function apiRequest<T>(path:string,init?:RequestInit):Promise<T>{if(!baseUrl)throw new ApiError("The website service is not configured. Please try again later.");const controller=new AbortController();const timeout=setTimeout(()=>controller.abort(),10_000);try{const response=await fetch(`${baseUrl}${path}`,{...init,headers:{"Content-Type":"application/json",...init?.headers},signal:controller.signal});let body:unknown;try{body=await response.json()}catch{body=null}if(!response.ok){const message=body&&typeof body==="object"&&"message" in body&&typeof body.message==="string"?body.message:"The request could not be completed.";throw new ApiError(message,response.status)}if(!body||typeof body!=="object"||!("data" in body))throw new ApiError("The service returned an invalid response.");return (body as {data:T}).data}catch(error){if(error instanceof ApiError)throw error;if(error instanceof DOMException&&error.name==="AbortError")throw new ApiError("The request timed out. Please try again.");throw new ApiError("The service is unavailable. Please check your connection and try again.")}finally{clearTimeout(timeout)}}
