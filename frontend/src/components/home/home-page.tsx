"use client";
import {motion,useReducedMotion} from "framer-motion";
import {ArrowDown,ArrowRight,Quote} from "lucide-react";
import Link from "next/link";
import {QuoteForm} from "@/components/forms/quote-form";
import {Reveal,staggerContainer,staggerItem} from "@/components/motion/reveal";
import {ApprovedIcon} from "@/components/ui/approved-icon";
import {LinkButton} from "@/components/ui/button";
import {SectionHeading} from "@/components/ui/section-heading";
import type {HomeCmsContent} from "@/content/home-cms";
export function HomePage({content}:{content:HomeCmsContent}){const reduce=useReducedMotion();return <><section className="hero-section"><div className="hero-glow"/><motion.div className="site-container relative z-10 max-w-5xl text-center" initial={reduce?false:"hidden"} animate="visible" variants={staggerContainer}><motion.p variants={staggerItem} className="hero-pill"><span/>{content.hero.eyebrow}</motion.p><motion.h1 variants={staggerItem} className="hero-title mt-9">{content.hero.title}<br/><span>{content.hero.accent}</span></motion.h1><motion.p variants={staggerItem} className="mx-auto mt-8 max-w-2xl text-base leading-7 text-secondary md:text-lg">{content.hero.description}</motion.p><motion.div variants={staggerItem} className="mt-10 flex flex-col justify-center gap-4 sm:flex-row"><LinkButton href="#services" className="rounded-full px-8 uppercase tracking-[.06em]">{content.hero.primaryButton}</LinkButton><LinkButton href="#portfolio" variant="secondary" className="rounded-full px-8 uppercase tracking-[.06em]">{content.hero.secondaryButton}</LinkButton></motion.div></motion.div><a href="#services" className="scroll-cue"><span>{content.hero.scrollLabel}</span><ArrowDown/></a></section><section id="services" className="section bg-charcoal"><div className="site-container"><Reveal><SectionHeading eyebrow={content.servicesHeading.eyebrow} title={content.servicesHeading.title} centered/></Reveal><motion.div className="mt-16 grid gap-5 md:grid-cols-2 lg:grid-cols-3" initial={reduce?false:"hidden"} whileInView="visible" viewport={{once:true,amount:.1}} variants={staggerContainer}>{content.services.filter(item=>item.visible).sort((a,b)=>a.order-b.order).map(item=><motion.div variants={staggerItem} key={item.href}><Link className="service-card" href={item.href}><span className="icon-box"><ApprovedIcon iconKey={item.iconKey}/></span><h3>{item.title}</h3><p>{item.description}</p></Link></motion.div>)}</motion.div><Reveal><div className="stats-panel">{content.stats.filter(item=>item.visible).sort((a,b)=>a.order-b.order).map(stat=><div className="stat" key={stat.label}><p>{stat.value}{stat.suffix}</p><span>{stat.label}</span></div>)}</div></Reveal></div></section><section id="portfolio" className="section bg-black"><div className="site-container"><Reveal><SectionHeading {...content.portfolioHeading}/></Reveal><motion.div className="portfolio-track" initial={reduce?false:"hidden"} whileInView="visible" viewport={{once:true,amount:.15}} variants={staggerContainer}>{content.projects.filter(item=>item.visible).sort((a,b)=>a.order-b.order).map(project=><motion.article variants={staggerItem} className="project-card" key={project.href}><div className={`project-art project-art-${project.tone}`}/><div className="project-info"><span>{project.category}</span><div><h3>{project.title}</h3><Link href={project.href} aria-label={`View ${project.title}`}><ArrowRight/></Link></div></div></motion.article>)}</motion.div><div className="portfolio-dots"><span/><i/><i/><i/></div></div></section><section className="section bg-charcoal"><div className="site-container max-w-4xl"><Reveal><h2 className="section-title text-center">{content.testimonialHeading}</h2><article className="testimonial-card"><Quote className="quote-mark"/><blockquote>“{content.testimonial.quote}”</blockquote><div className="avatar" aria-hidden="true">{content.testimonial.name.split(/\s+/).map(item=>item[0]).join("").slice(0,2)}</div><h3>{content.testimonial.name}</h3><p>{content.testimonial.role}</p><div className="stars" aria-label={`${content.testimonial.rating} out of 5 stars`}>{"★".repeat(content.testimonial.rating)}</div></article><p className="mt-8 text-center text-xs text-muted">{content.testimonial.pauseLabel}</p></Reveal></div></section>{content.quote.visible&&<section id="quote" className="quote-section"><div className="site-container max-w-4xl"><Reveal><SectionHeading eyebrow={content.quote.eyebrow} title={content.quote.title} centered/><div className="mt-14"><QuoteForm/></div></Reveal></div></section>}</>}
