// Shared timing for every auto-sliding carousel on the site (Featured Work,
// Testimonials, ...). Keeping this in one place means their speeds can't
// silently drift apart again -- change it here and every carousel follows.
export const AUTO_SLIDE_INTERVAL_MS = 4500;
